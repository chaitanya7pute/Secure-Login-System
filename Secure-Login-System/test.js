// Task 4: Secure Login System.

    // const → Declares a constant variable (cannot be reassigned).
    // db -> It will hold whatever is imported from ./db.
    // require('./db') -> This imports the db connection object from the db.js file in the same directory.
const db = require('./db');
